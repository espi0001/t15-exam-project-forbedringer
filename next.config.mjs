/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["lively-scrawny-secretary.glitch.me", "localhost", "picsum.photos"], // Domæner jeg han hente bilelder fra
  },
};

export default nextConfig;
